MANIFOLD_URL=http://localhost:8000
# Server-side only — enables draft previews. Create one with:
#   php artisan tinker --execute='echo App\Models\User::first()->createToken("preview")->plainTextToken;'
MANIFOLD_SERVER_TOKEN=
